version: 0.2

phases:
  install:
    commands:
      - |
        echo 'install';
        ./test.sh;
        echo 'install';
        ./test.sh;
        echo 'install';
        ./test.sh;
        echo 'install';
  pre_build:
    commands:
      - |
        echo 'pre_build';
        ./test.sh;
  build:
    commands:
      - |
        echo 'build';
        ./test.sh;
  post_build:
    commands:
      - |
        echo 'post_build';
        ./test.sh;
