version: 0.2

phases:
  install:
    commands:
      - |
        set -eu;
        echo 'install';
        ./test.sh;
  pre_build:
    commands:
      - |
        set -eu;
        echo 'pre_build';
        ./test.sh;
  build:
    commands:
      - |
        set -eu;
        echo 'build';
        ./test.sh;
  post_build:
    commands:
      - |
        set -eu;
        echo 'post_build';
        ./test.sh;

